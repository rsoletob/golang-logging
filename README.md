Logging library that uses github.com/Sirupsen/logrus to write json lines with more standard fields for strutured logging.
Also supports plain line standard mode if env variable ```DEBUG``` is set to ```1```